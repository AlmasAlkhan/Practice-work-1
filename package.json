{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "TaskFlow — Industry Practice Project by Alkhan Almas",
  "private": true,
  "scripts": {
    "sync": "rm -rf backend/frontend && cp -R frontend backend/frontend",
    "build": "npm install --prefix backend && npm run sync",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "marked": "^18.0.5",
    "playwright": "^1.61.1"
  }
}
